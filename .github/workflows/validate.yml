name: Validate commits

on:
  pull_request:
    paths:
        - "slides.md"
        - "pages/**"

  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  validate:
    uses: ./.github/workflows/build.yml
